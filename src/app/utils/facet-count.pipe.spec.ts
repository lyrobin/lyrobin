import { FacetCountPipe } from './facet-count.pipe';

describe('FacetCountPipe', () => {
  it('create an instance', () => {
    const pipe = new FacetCountPipe();
    expect(pipe).toBeTruthy();
  });
});
