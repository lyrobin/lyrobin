import { FacetValuePipe } from './facet-value.pipe';

describe('FacetValuePipe', () => {
  it('create an instance', () => {
    const pipe = new FacetValuePipe();
    expect(pipe).toBeTruthy();
  });
});
