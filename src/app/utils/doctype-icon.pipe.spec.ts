import { DoctypeIconPipe } from './doctype-icon.pipe';

describe('DoctypeIconPipe', () => {
  it('create an instance', () => {
    const pipe = new DoctypeIconPipe();
    expect(pipe).toBeTruthy();
  });
});
